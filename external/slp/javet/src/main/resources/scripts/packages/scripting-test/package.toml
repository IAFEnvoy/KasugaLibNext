name = "@test/scripting-test"
engine = "javascript"
version = "1.0.0"
main = "index"

[entry]
# Client-only: index.js calls ScriptingTestApi.printText, which touches net.minecraft.client.Minecraft.
# Running as `common` crashed a dedicated server (client class on DEDICATED_SERVER dist).
client = ["index"]
